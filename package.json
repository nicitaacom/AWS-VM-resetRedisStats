{
  "description": "This is code that fetch code and execute it",
    "scripts": {
    "build": "rm -rf VM-resetRedisStats.zip && tsc && zip -r VM-resetRedisStats.zip freeEmailList.txt index.js node_modules"
  },
  "devDependencies": {
    "@types/mailparser": "^3.4.5",
    "@types/node": "^22.7.4",
    "i": "^0.3.7",
    "npm": "^11.0.0"
  },
  "dependencies": {
    "ioredis": "^5.4.2",
    "moment-timezone": "^0.6.0",
    "vm2": "^3.9.16"
  }
}
